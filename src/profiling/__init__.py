# TODO(mvp, milestone 4): standalone CLI script that precomputes a data profile
# (cardinality, nulls, ranges, distributions, orphans) to a JSON artifact. Never runs
# live during a session. See spec section 7 ("Redshift").
