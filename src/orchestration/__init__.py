# TODO(mvp, milestone 3): CrewAI Flow lives here (lead loop + architect/domain_expert/
# analyst crews called from flow steps). This is the ONLY package allowed to
# `from crewai import ...` — prompts, artifact schema (src/models) and the renderer
# (src/render) must survive a framework swap. See spec section 7.
