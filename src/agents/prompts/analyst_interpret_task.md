Hypotéza k ověření: {hypothesis}

Dotaz, který steward schválil a který proběhl: {query_description}

Výsledek dotazu:

{result_summary}

Zformuluj nález. `text` napiš jako vlastní nejistotu, ne jako přistižení stewarda v
chybě. `quantification` uveď konkrétně (počet/podíl). Pokud výsledek hypotézu jasně
potvrzuje nebo vyvrací, nastav `confirms_hypothesis` na true/false; pokud je výsledek
nejednoznačný, nastav `null` a řekni proč. `suggested_threshold` vyplň jen pokud dává
smysl navrhnout práh pro budoucí pravidlo kvality na základě této naměřené baseline —
jinak nech `null`.
