# TODO(mvp, milestone 4): read-only Redshift tool. SQL is always shown to the steward
# and approved before it runs; read-only role, curated layers only (not `prep`),
# SELECT whitelist, statement_timeout, and a programmatically enforced LIMIT.
# See spec section 7 ("Redshift") and section 8 ("Bezpecnost").
