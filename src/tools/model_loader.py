# TODO(mvp, milestone 2): loads the physical model JSON export and deterministically
# filters it down to a domain's slice by config/domains/<domain>.yaml table_prefixes.
# No LLM involved in the filtering step — see spec section 10.
