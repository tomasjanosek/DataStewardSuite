pydantic>=2.7,<3
PyYAML>=6.0,<7
GitPython>=3.1,<4
pytest>=8.0,<9

# TODO(mvp): added in later milestones — crewai (orchestration), streamlit (ui),
# boto3 (Bedrock), redshift-connector or psycopg (analyst/Redshift tool).
