pydantic>=2.7,<3
PyYAML>=6.0,<7
GitPython>=3.1,<4
pytest>=8.0,<9
anthropic>=0.40,<1
crewai>=0.80,<1

# TODO(mvp): added in later milestones — streamlit (ui), boto3 (Bedrock),
# redshift-connector or psycopg (analyst/Redshift tool).
